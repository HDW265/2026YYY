.版本 2

' ============================================================
' 体积仍大的原因：
' 1) 实际存的是 .bmp（约 4.3MB），不是压缩 JPEG
' 2) 或 if/否则 仍写错，压缩失败被静默跳过
'
' 本版压缩改为更稳的流程：
'   写临时.bmp → GDI+ 读文件 → 另存为 .jpg → 删临时文件
' ============================================================

.DLL命令 GdiplusStartup, 整数型, "gdiplus", "GdiplusStartup"
    .参数 token, 整数型, , 传址
    .参数 input, GdiplusStartupInput, , 传址
    .参数 output, 整数型

.DLL命令 GdiplusShutdown, , "gdiplus", "GdiplusShutdown"
    .参数 token, 整数型

.DLL命令 GdipLoadImageFromFile, 整数型, "gdiplus", "GdipLoadImageFromFile"
    .参数 filename, 字节集
    .参数 image, 整数型, , 传址

.DLL命令 GdipDisposeImage, 整数型, "gdiplus", "GdipDisposeImage"
    .参数 image, 整数型

.DLL命令 GdipSaveImageToFile, 整数型, "gdiplus", "GdipSaveImageToFile"
    .参数 image, 整数型
    .参数 filename, 字节集
    .参数 clsidEncoder, 字节集
    .参数 encoderParams, 整数型

.数据类型 GdiplusStartupInput, 公开
    .成员 GdiplusVersion, 整数型
    .成员 DebugEventCallback, 整数型
    .成员 SuppressBackgroundThread, 整数型
    .成员 SuppressExternalCodecs, 整数型

.DLL命令 MultiByteToWideChar, 整数型, "kernel32", "MultiByteToWideChar"
    .参数 CodePage, 整数型
    .参数 dwFlags, 整数型
    .参数 lpMultiByteStr, 文本型
    .参数 cbMultiByte, 整数型
    .参数 lpWideCharStr, 字节集
    .参数 cchWideChar, 整数型


.子程序 _服务器_局域网监控系统_数据到达
.局部变量 数据, 字节集
.局部变量 缓冲长度, 整数型

.如果真 (是否接收 ≠ 真)
    返回 ()
.如果真结束

数据 ＝ 服务器_局域网监控系统.取回数据 ()
.如果真 (取字节集长度 (数据) ＜ 1)
    返回 ()
.如果真结束

收图缓冲 ＝ 收图缓冲 ＋ 数据
缓冲长度 ＝ 取字节集长度 (收图缓冲)
.如果真 (缓冲长度 ＞ 12000000)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束
.如果真 (缓冲长度 ＜ 20000)
    返回 ()
.如果真结束

屏幕图片 ＝ 载入图片 (收图缓冲)
.如果真 (屏幕图片 ＝ 0)
    返回 ()
.如果真结束

.如果真 (取启动时间 () － 上次刷新时间 ＜ 120)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束
上次刷新时间 ＝ 取启动时间 ()

画板_显示屏幕信息.底图 ＝ 收图缓冲

.如果真 (是否保存图片 ≠ 真)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束
.如果真 (保存间隔毫秒 ＜ 1)
    保存间隔毫秒 ＝ 1000
.如果真结束
.如果真 (取启动时间 () － 上次保存时间 ＜ 保存间隔毫秒)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束

.如果真 (保存目录 ＝ “”)
    保存目录 ＝ “E:\旧硬盘资料\原创实例\局域网\SF\”
.如果真结束
.如果真 (取文本右边 (保存目录, 1) ≠ “\”)
    保存目录 ＝ 保存目录 ＋ “\”
.如果真结束
.如果真 (保存序号 ＜ 1)
    保存序号 ＝ 1
.如果真结束
.如果真 (保存JPEG质量 ＜ 1 或 保存JPEG质量 ＞ 100)
    保存JPEG质量 ＝ 60
.如果真结束

' ★ 必须用 .否则，成功和失败只能走一边
保存路径 ＝ 保存目录 ＋ 到文本 (保存序号) ＋ “.jpg”
.如果真 (监控收图_压缩保存JPEG (收图缓冲, 保存路径, 保存JPEG质量))
    输出调试文本 (“JPEG保存成功：” ＋ 保存路径 ＋ “ 大小=” ＋ 到文本 (取文件尺寸 (保存路径)))
.否则
    保存路径 ＝ 保存目录 ＋ 到文本 (保存序号) ＋ “.bmp”
    写到文件 (保存路径, 收图缓冲)
    输出调试文本 (“JPEG失败，已原样保存：” ＋ 保存路径 ＋ “ 大小=” ＋ 到文本 (取字节集长度 (收图缓冲)))
.如果真结束

保存序号 ＝ 保存序号 ＋ 1
上次保存时间 ＝ 取启动时间 ()
收图缓冲 ＝ {  }


.子程序 监控收图_压缩保存JPEG, 逻辑型
.参数 图片数据, 字节集
.参数 文件路径, 文本型
.参数 质量, 整数型, 可空
.局部变量 token, 整数型
.局部变量 startup, GdiplusStartupInput
.局部变量 pImage, 整数型
.局部变量 临时bmp, 文本型
.局部变量 宽临时, 字节集
.局部变量 宽目标, 字节集
.局部变量 encClsid, 字节集
.局部变量 数据长度, 整数型
.局部变量 返回值, 逻辑型
.局部变量 结果大小, 整数型

返回值 ＝ 假
.如果真 (是否为空 (质量) 或 质量 ＜ 1 或 质量 ＞ 100)
    质量 ＝ 60
.如果真结束

数据长度 ＝ 取字节集长度 (图片数据)
.如果真 (数据长度 ＜ 100)
    返回 (假)
.如果真结束

' 已是较小 JPEG，直接写
.如果真 (取字节集左边 (图片数据, 2) ＝ { 255, 216 } 且 数据长度 ＜ 1500000)
    写到文件 (文件路径, 图片数据)
    返回 (真)
.如果真结束

' 1) 先落到临时 bmp（写到文件已验证可用）
临时bmp ＝ 文件路径 ＋ “.tmp.bmp”
写到文件 (临时bmp, 图片数据)

' 2) GDI+ 从文件加载再另存 jpg
startup.GdiplusVersion ＝ 1
startup.DebugEventCallback ＝ 0
startup.SuppressBackgroundThread ＝ 0
startup.SuppressExternalCodecs ＝ 0
token ＝ 0
.如果真 (GdiplusStartup (token, startup, 0) ≠ 0)
    删除文件 (临时bmp)
    返回 (假)
.如果真结束

宽临时 ＝ 监控收图_路径到Unicode (临时bmp)
pImage ＝ 0
.如果真 (GdipLoadImageFromFile (宽临时, pImage) ≠ 0 或 pImage ＝ 0)
    GdiplusShutdown (token)
    删除文件 (临时bmp)
    返回 (假)
.如果真结束

宽目标 ＝ 监控收图_路径到Unicode (文件路径)
encClsid ＝ { 1, 244, 124, 85, 4, 26, 211, 17, 154, 115, 0, 0, 248, 30, 243, 46 }
.如果真 (GdipSaveImageToFile (pImage, 宽目标, encClsid, 0) ＝ 0)
    结果大小 ＝ 取文件尺寸 (文件路径)
    ' 仍然小于 1.5MB 才算压缩成功（你的原图约 4.3MB）
    .如果真 (结果大小 ＞ 1000 且 结果大小 ＜ 1500000)
        返回值 ＝ 真
    .如果真结束
.如果真结束

GdipDisposeImage (pImage)
GdiplusShutdown (token)
删除文件 (临时bmp)

.如果真 (返回值 ≠ 真)
    ' 失败时删掉可能产生的异常大 jpg
    .如果真 (取文件尺寸 (文件路径) ≥ 1500000)
        删除文件 (文件路径)
    .如果真结束
.如果真结束

返回 (返回值)


.子程序 监控收图_路径到Unicode, 字节集
.参数 路径, 文本型
.局部变量 宽, 字节集
.局部变量 字符数, 整数型
.局部变量 i, 整数型

字符数 ＝ 取文本长度 (路径) ＋ 1
宽 ＝ {  }
.计次循环首 (字符数 × 2, i)
    宽 ＝ 宽 ＋ { 0 }
.计次循环尾 ()
MultiByteToWideChar (0, 0, 路径, -1, 宽, 字符数)
返回 (宽)
