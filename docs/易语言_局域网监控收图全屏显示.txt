.版本 2

' ============================================================
' 画板没有「拉伸」时的做法（你下拉里只有：左上/平铺/居中）
'
' 推荐：用「图片框」显示画面（有拉伸），画板可留着当底，也可删掉画板改用图片框
'
' 【操作】
' 1. 在窗口上放图片框，名称：图片框_显示屏幕信息
' 2. 大小位置尽量与原来画板一致（可先盖在画板上）
' 3. 属性「显示方式」选「拉伸图片」（有的版本叫：比例拉伸/拉伸）
' 4. 下面「数据到达」里用：图片框_显示屏幕信息.图片 ＝ 帧数据
'
' 若坚持用画板：底图方式只能左上/平铺/居中，无法铺满；
' 只能显示原图像素，或继续用左移/上移挪图，不能指望底图方式拉伸。
' ============================================================

.DLL命令 RtlMoveMemory_整数, , "kernel32", "RtlMoveMemory"
    .参数 整数目标, 整数型, , 传址
    .参数 字节来源, 字节集
    .参数 长度, 整数型


' 程序集变量（保留你工程里已有的即可）
' .程序集变量 是否接收, 逻辑型
' .程序集变量 收图缓冲, 字节集
' .程序集变量 上次刷新时间, 整数型
' .程序集变量 最近一帧, 字节集


.子程序 _服务器_局域网监控系统_数据到达
.局部变量 数据, 字节集
.局部变量 缓冲长度, 整数型
.局部变量 帧数据, 字节集

.如果真 (是否接收 ≠ 真)
    返回 ()
.如果真结束

数据 ＝ 服务器_局域网监控系统.取回数据 ()
.如果真 (取字节集长度 (数据) ＜ 1)
    返回 ()
.如果真结束

收图缓冲 ＝ 收图缓冲 ＋ 数据
缓冲长度 ＝ 取字节集长度 (收图缓冲)
.如果真 (缓冲长度 ＞ 12000000)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束

帧数据 ＝ 监控收图_取出完整帧 ()
.如果真 (取字节集长度 (帧数据) ＜ 1)
    返回 ()
.如果真结束

.如果真 (取启动时间 () － 上次刷新时间 ＜ 120)
    返回 ()
.如果真结束
上次刷新时间 ＝ 取启动时间 ()
最近一帧 ＝ 帧数据

' ★★★ 就改这里（收齐完整帧之后）★★★
' 方案A（推荐，能铺满）：图片框 + 显示方式=拉伸图片
图片框_显示屏幕信息.图片 ＝ 帧数据

' 方案B（继续用画板，不能拉伸铺满，只更新底图）：
' 画板_显示屏幕信息.底图 ＝ 帧数据


.子程序 监控收图_取出完整帧, 字节集
.局部变量 缓冲长度, 整数型
.局部变量 起点, 整数型
.局部变量 终点, 整数型
.局部变量 帧长, 整数型
.局部变量 帧数据, 字节集
.局部变量 头两字节, 字节集
.局部变量 长度字节, 字节集

帧数据 ＝ {  }
缓冲长度 ＝ 取字节集长度 (收图缓冲)
.如果真 (缓冲长度 ＜ 4)
    返回 (帧数据)
.如果真结束

起点 ＝ 寻找字节集 (收图缓冲, { 255, 216 }, 1)
.如果真 (起点 ＞ 0)
    .如果真 (起点 ＞ 1)
        收图缓冲 ＝ 取字节集右边 (收图缓冲, 缓冲长度 － 起点 ＋ 1)
        缓冲长度 ＝ 取字节集长度 (收图缓冲)
    .如果真结束
    终点 ＝ 寻找字节集 (收图缓冲, { 255, 217 }, 3)
    .如果真 (终点 ＞ 0)
        帧数据 ＝ 取字节集左边 (收图缓冲, 终点 ＋ 1)
        .如果真 (终点 ＋ 1 ＜ 缓冲长度)
            收图缓冲 ＝ 取字节集右边 (收图缓冲, 缓冲长度 － 终点 － 1)
        .否则
            收图缓冲 ＝ {  }
        .如果真结束
        返回 (帧数据)
    .如果真结束
    返回 (帧数据)
.如果真结束

.如果真 (缓冲长度 ≥ 6)
    头两字节 ＝ 取字节集左边 (收图缓冲, 2)
    .如果真 (头两字节 ＝ { 66, 77 })
        长度字节 ＝ 取字节集中间 (收图缓冲, 3, 4)
        帧长 ＝ 0
        RtlMoveMemory_整数 (帧长, 长度字节, 4)
        .如果真 (帧长 ＞ 54 且 帧长 ≤ 缓冲长度)
            帧数据 ＝ 取字节集左边 (收图缓冲, 帧长)
            .如果真 (帧长 ＜ 缓冲长度)
                收图缓冲 ＝ 取字节集右边 (收图缓冲, 缓冲长度 － 帧长)
            .否则
                收图缓冲 ＝ {  }
            .如果真结束
            返回 (帧数据)
        .如果真结束
        .如果真 (帧长 ＜ 54 或 帧长 ＞ 12000000)
            收图缓冲 ＝ 取字节集右边 (收图缓冲, 缓冲长度 － 1)
        .如果真结束
        返回 (帧数据)
    .如果真结束
.如果真结束

.如果真 (缓冲长度 ＞ 65536)
    收图缓冲 ＝ {  }
.如果真结束
返回 (帧数据)
