.版本 2

' ============================================================
' 基于你原来的「数据到达」最小改法
'
' 【程序集变量】请确认有这些（不要再用局部变量拼包）：
' .程序集变量 是否接收, 逻辑型
' .程序集变量 屏幕图片, 整数型
' .程序集变量 收图缓冲, 字节集
' .程序集变量 上次刷新时间, 整数型
' .程序集变量 是否保存图片, 逻辑型
' .程序集变量 保存目录, 文本型
' .程序集变量 保存序号, 整数型
'
' 【创建完毕】里加：
' 是否保存图片 ＝ 真
' 保存目录 ＝ “E:\旧硬盘资料\原创实例\局域网\”
' 保存序号 ＝ 1
' ============================================================

.子程序 _服务器_局域网监控系统_数据到达
.局部变量 数据, 字节集
.局部变量 缓冲长度, 整数型
.局部变量 保存路径, 文本型

.如果真 (是否接收 ≠ 真)
    返回 ()
.如果真结束

数据 ＝ 服务器_局域网监控系统.取回数据 ()
.如果真 (取字节集长度 (数据) ＜ 1)
    返回 ()
.如果真结束

' 拼包：半包先攒着，禁止每包都 底图＝数据
' ★ 收图缓冲 必须是程序集变量，不能写在局部变量里
收图缓冲 ＝ 收图缓冲 ＋ 数据
缓冲长度 ＝ 取字节集长度 (收图缓冲)

.如果真 (缓冲长度 ＞ 12000000)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束

.如果真 (缓冲长度 ＜ 20000)
    返回 ()
.如果真结束

屏幕图片 ＝ 载入图片 (收图缓冲)
.如果真 (屏幕图片 ＝ 0)
    ' 还不是完整图：不改底图，保留上一帧
    返回 ()
.如果真结束

' 降频（可选）；★ 上次刷新时间 也必须是程序集变量
.如果真 (取启动时间 () － 上次刷新时间 ＜ 120)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束
上次刷新时间 ＝ 取启动时间 ()

' 载入成功才显示
画板_显示屏幕信息.底图 ＝ 收图缓冲

' ---------- 批量保存（替掉原来写死的 1.jpg）----------
.如果真 (是否保存图片 ＝ 真)
    .如果真 (保存目录 ＝ “”)
        保存目录 ＝ “E:\旧硬盘资料\原创实例\局域网\”
    .如果真结束
    .如果真 (取文本右边 (保存目录, 1) ≠ “\”)
        保存目录 ＝ 保存目录 ＋ “\”
    .如果真结束
    .如果真 (保存序号 ＜ 1)
        保存序号 ＝ 1
    .如果真结束

    ' 原来：写到文件 (“E:\...\1.jpg”, 收图缓冲)  ← 只会覆盖同一张
    ' 现在：1.jpg 2.jpg 3.jpg ...
    保存路径 ＝ 保存目录 ＋ 到文本 (保存序号) ＋ “.jpg”
    写到文件 (保存路径, 收图缓冲)
    保存序号 ＝ 保存序号 ＋ 1
.如果真结束

收图缓冲 ＝ {  }
