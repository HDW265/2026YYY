.版本 2

' ============================================================
' 排查：调试成功/失败都没有 → 没进保存分支，或未粘贴带调试的代码
' 本版先去掉 GDI+，直接 写到文件，并在关键点输出调试文本
'
' 创建完毕请写成：
' 是否保存图片 ＝ 真
' 保存目录 ＝ “E:\旧硬盘资料\原创实例\局域网\SF\”
' 保存序号 ＝ 1
' 保存间隔毫秒 ＝ 1000
' 上次保存时间 ＝ 0
' ============================================================

.子程序 _服务器_局域网监控系统_数据到达
.局部变量 数据, 字节集
.局部变量 缓冲长度, 整数型
.局部变量 时间差, 整数型

.如果真 (是否接收 ≠ 真)
    返回 ()
.如果真结束

数据 ＝ 服务器_局域网监控系统.取回数据 ()
.如果真 (取字节集长度 (数据) ＜ 1)
    返回 ()
.如果真结束

收图缓冲 ＝ 收图缓冲 ＋ 数据
缓冲长度 ＝ 取字节集长度 (收图缓冲)

.如果真 (缓冲长度 ＞ 12000000)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束

.如果真 (缓冲长度 ＜ 20000)
    返回 ()
.如果真结束

屏幕图片 ＝ 载入图片 (收图缓冲)
.如果真 (屏幕图片 ＝ 0)
    返回 ()
.如果真结束

.如果真 (取启动时间 () － 上次刷新时间 ＜ 120)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束
上次刷新时间 ＝ 取启动时间 ()

画板_显示屏幕信息.底图 ＝ 收图缓冲
输出调试文本 (“已显示 大小=” ＋ 到文本 (缓冲长度) ＋ “ 是否保存=” ＋ 选择 (是否保存图片, “真”, “假”))

' ---------- 先用写到文件，确认能落盘 ----------
.如果真 (是否保存图片 ≠ 真)
    输出调试文本 (“未保存：是否保存图片不为真”)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束

.如果真 (保存间隔毫秒 ＜ 1)
    保存间隔毫秒 ＝ 1000
.如果真结束

时间差 ＝ 取启动时间 () － 上次保存时间
输出调试文本 (“保存判断 时间差=” ＋ 到文本 (时间差) ＋ “ 间隔=” ＋ 到文本 (保存间隔毫秒))

.如果真 (时间差 ＜ 保存间隔毫秒)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束

.如果真 (保存目录 ＝ “”)
    保存目录 ＝ “E:\旧硬盘资料\原创实例\局域网\SF\”
.如果真结束
.如果真 (取文本右边 (保存目录, 1) ≠ “\”)
    保存目录 ＝ 保存目录 ＋ “\”
.如果真结束
.如果真 (保存序号 ＜ 1)
    保存序号 ＝ 1
.如果真结束

保存路径 ＝ 保存目录 ＋ 到文本 (保存序号) ＋ “.bmp”
写到文件 (保存路径, 收图缓冲)
输出调试文本 (“已写到文件：” ＋ 保存路径)

保存序号 ＝ 保存序号 ＋ 1
上次保存时间 ＝ 取启动时间 ()
收图缓冲 ＝ {  }
