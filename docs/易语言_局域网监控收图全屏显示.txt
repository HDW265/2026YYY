.版本 2

' ============================================================
' 显示正常但目录没图：压缩保存失败时会静默跳过
' 本版：创建目录 + 拼路径 + 压缩失败则写到文件兜底 + 调试输出
' ============================================================

.子程序 _服务器_局域网监控系统_数据到达
.局部变量 数据, 字节集
.局部变量 缓冲长度, 整数型
.局部变量 已保存, 逻辑型

.如果真 (是否接收 ≠ 真)
    返回 ()
.如果真结束

数据 ＝ 服务器_局域网监控系统.取回数据 ()
.如果真 (取字节集长度 (数据) ＜ 1)
    返回 ()
.如果真结束

收图缓冲 ＝ 收图缓冲 ＋ 数据
缓冲长度 ＝ 取字节集长度 (收图缓冲)

.如果真 (缓冲长度 ＞ 12000000)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束

.如果真 (缓冲长度 ＜ 20000)
    返回 ()
.如果真结束

屏幕图片 ＝ 载入图片 (收图缓冲)
.如果真 (屏幕图片 ＝ 0)
    返回 ()
.如果真结束

.如果真 (取启动时间 () － 上次刷新时间 ＜ 120)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束
上次刷新时间 ＝ 取启动时间 ()

画板_显示屏幕信息.底图 ＝ 收图缓冲

' ---------- 批量保存 ----------
.如果真 (是否保存图片 ＝ 真)
    .如果真 (保存间隔毫秒 ＜ 1)
        保存间隔毫秒 ＝ 1000
    .如果真结束

    ' 测试时可先改成 1000；你现在若是 30000，要等 30 秒才存第一张
    .如果真 (取启动时间 () － 上次保存时间 ≥ 保存间隔毫秒)
        .如果真 (保存目录 ＝ “”)
            保存目录 ＝ “E:\旧硬盘资料\原创实例\局域网\SF\”
        .如果真结束
        .如果真 (取文本右边 (保存目录, 1) ≠ “\”)
            保存目录 ＝ 保存目录 ＋ “\”
        .如果真结束
        .如果真 (保存序号 ＜ 1)
            保存序号 ＝ 1
        .如果真结束
        .如果真 (保存JPEG质量 ＜ 1 或 保存JPEG质量 ＞ 100)
            保存JPEG质量 ＝ 60
        .如果真结束

        ' 目录不存在会保存失败（请先建好，或用下面创建目录）
        创建目录 (保存目录)

        已保存 ＝ 假
        保存路径 ＝ 保存目录 ＋ 到文本 (保存序号) ＋ “.jpg”

        .如果真 (监控收图_压缩保存JPEG (收图缓冲, 保存路径, 保存JPEG质量))
            已保存 ＝ 真
            输出调试文本 (“JPEG保存成功：” ＋ 保存路径)
        .否则
            ' 压缩失败：原样落盘，避免一张都存不下
            保存路径 ＝ 保存目录 ＋ 到文本 (保存序号) ＋ “.bmp”
            写到文件 (保存路径, 收图缓冲)
            已保存 ＝ 真
            输出调试文本 (“JPEG失败，已原样保存：” ＋ 保存路径 ＋ “ 大小=” ＋ 到文本 (取字节集长度 (收图缓冲)))
        .如果真结束

        .如果真 (已保存)
            保存序号 ＝ 保存序号 ＋ 1
            上次保存时间 ＝ 取启动时间 ()
        .如果真结束
    .如果真结束
.如果真结束

收图缓冲 ＝ {  }
