.版本 2

.程序集 窗口程序集_启动窗口
.程序集变量 屏幕图片, 整数型
.程序集变量 图片宽度, 整数型
.程序集变量 图片高度, 整数型
.程序集变量 J_客户IP, 文本型
.程序集变量 J_屏幕宽度, 整数型
.程序集变量 J_屏幕高度, 整数型
.程序集变量 是否接收, 逻辑型
.程序集变量 收图缓冲, 字节集
.程序集变量 上次刷新时间, 整数型
.程序集变量 最近一帧, 字节集
' 已删除：全屏铺满方式（不再用 StretchBlt）


.子程序 __启动窗口_创建完毕

服务器_局域网监控系统.端口 ＝ 到整数 (_启动窗口.端口_编辑框)
是否接收 ＝ 真

J_屏幕宽度 ＝ 取屏幕宽度 ()
J_屏幕高度 ＝ 取屏幕高度 ()
画板_显示屏幕信息.顶边 ＝ 10
画板_显示屏幕信息.左边 ＝ 10

' ★ 全屏关键：把「图片居左上」改成拉伸
' 若编译提示常量不对，请在设计器属性里手动选「拉伸」，并删掉下面这行
画板_显示屏幕信息.底图方式 ＝ 2

输出调试文本 (是否接收)
组件位置大小调整 ()


.子程序 _按钮_系统退出_被单击

全部关闭 ()
结束 ()


.子程序 _服务器_局域网监控系统_数据到达
.局部变量 数据, 字节集
.局部变量 缓冲长度, 整数型
.局部变量 帧数据, 字节集

.如果真 (是否接收 ≠ 真)
    返回 ()
.如果真结束

数据 ＝ 服务器_局域网监控系统.取回数据 ()
.如果真 (取字节集长度 (数据) ＜ 1)
    返回 ()
.如果真结束

收图缓冲 ＝ 收图缓冲 ＋ 数据
缓冲长度 ＝ 取字节集长度 (收图缓冲)

.如果真 (缓冲长度 ＞ 12000000)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束

帧数据 ＝ 监控收图_取出完整帧 ()
.如果真 (取字节集长度 (帧数据) ＜ 1)
    返回 ()
.如果真结束

.如果真 (取启动时间 () － 上次刷新时间 ＜ 120)
    返回 ()
.如果真结束
上次刷新时间 ＝ 取启动时间 ()
最近一帧 ＝ 帧数据

' ★ 只赋底图，禁止 载入图片 / StretchBlt / DeleteObject（会闪退）
画板_显示屏幕信息.底图 ＝ 帧数据


.子程序 监控收图_取出完整帧, 字节集
.局部变量 缓冲长度, 整数型
.局部变量 起点, 整数型
.局部变量 终点, 整数型
.局部变量 帧长, 整数型
.局部变量 帧数据, 字节集
.局部变量 头两字节, 字节集
.局部变量 长度字节, 字节集

帧数据 ＝ {  }
缓冲长度 ＝ 取字节集长度 (收图缓冲)
.如果真 (缓冲长度 ＜ 4)
    返回 (帧数据)
.如果真结束

' JPEG：FFD8 ... FFD9
起点 ＝ 寻找字节集 (收图缓冲, { 255, 216 }, 1)
.如果真 (起点 ＞ 0)
    .如果真 (起点 ＞ 1)
        收图缓冲 ＝ 取字节集右边 (收图缓冲, 缓冲长度 － 起点 ＋ 1)
        缓冲长度 ＝ 取字节集长度 (收图缓冲)
    .如果真结束
    终点 ＝ 寻找字节集 (收图缓冲, { 255, 217 }, 3)
    .如果真 (终点 ＞ 0)
        帧数据 ＝ 取字节集左边 (收图缓冲, 终点 ＋ 1)
        .如果真 (终点 ＋ 1 ＜ 缓冲长度)
            收图缓冲 ＝ 取字节集右边 (收图缓冲, 缓冲长度 － 终点 － 1)
        .否则
            收图缓冲 ＝ {  }
        .如果真结束
        返回 (帧数据)
    .如果真结束
    返回 (帧数据)
.如果真结束

' BMP：BM + 文件长度
.如果真 (缓冲长度 ≥ 6)
    头两字节 ＝ 取字节集左边 (收图缓冲, 2)
    .如果真 (头两字节 ＝ { 66, 77 })
        长度字节 ＝ 取字节集中间 (收图缓冲, 3, 4)
        帧长 ＝ 0
        RtlMoveMemory_整数 (帧长, 长度字节, 4)
        .如果真 (帧长 ＞ 54 且 帧长 ≤ 缓冲长度)
            帧数据 ＝ 取字节集左边 (收图缓冲, 帧长)
            .如果真 (帧长 ＜ 缓冲长度)
                收图缓冲 ＝ 取字节集右边 (收图缓冲, 缓冲长度 － 帧长)
            .否则
                收图缓冲 ＝ {  }
            .如果真结束
            返回 (帧数据)
        .如果真结束
        .如果真 (帧长 ＜ 54 或 帧长 ＞ 12000000)
            收图缓冲 ＝ 取字节集右边 (收图缓冲, 缓冲长度 － 1)
        .如果真结束
        返回 (帧数据)
    .如果真结束
.如果真结束

.如果真 (缓冲长度 ＞ 65536)
    收图缓冲 ＝ {  }
.如果真结束
返回 (帧数据)


.子程序 _主窗口_可否被关闭, 逻辑型

_按钮_系统退出_被单击 ()


.子程序 _服务器_局域网监控系统_客户离开
.局部变量 客户IP, 文本型

客户IP ＝ 服务器_局域网监控系统.取回客户 ()
服务器_局域网监控系统.断开客户 (客户IP)
输出调试文本 (“用户【：” ＋ 客户IP ＋ “】连接已断开！”)


.子程序 _服务器_局域网监控系统_客户进入
.局部变量 客户IP, 文本型
.局部变量 纯IP, 文本型
.局部变量 位置, 整数型

客户IP ＝ 服务器_局域网监控系统.取回客户 ()
位置 ＝ 寻找文本 (客户IP, “:”, , 假)
.如果真 (位置 ≠ -1)
    纯IP ＝ 取文本左边 (客户IP, 位置 － 1)
.否则
    纯IP ＝ 客户IP
.如果真结束

.如果真 (IP是否允许 (纯IP) ＝ 假)
    服务器_局域网监控系统.断开客户 (客户IP)
    返回 ()
.如果真结束
J_客户IP ＝ 客户IP
输出调试文本 (“用户【” ＋ 客户IP ＋ “】已上线！！”)
编辑框_状态.内容 ＝ “用户【” ＋ 客户IP ＋ “】已上线！”


.子程序 取出纯IP, 文本型
.参数 地址, 文本型
.局部变量 i, 整数型
.局部变量 字, 文本型
.局部变量 结果, 文本型

地址 ＝ 删全部空 (地址)
结果 ＝ “”
.计次循环首 (取文本长度 (地址), i)
    字 ＝ 取文本中间 (地址, i, 1)
    .如果真 (字 ＝ “:” 或 字 ＝ “：”)
        跳出循环 ()
    .如果真结束
    结果 ＝ 结果 ＋ 字
.计次循环尾 ()
返回 (结果)


.子程序 IP是否允许, 逻辑型
.参数 待查IP, 文本型
.局部变量 列表, 文本型
.局部变量 规范化, 文本型
.局部变量 纯IP, 文本型

纯IP ＝ 取出纯IP (待查IP)
输出调试文本 (“待查=” ＋ 待查IP ＋ “ 纯IP=” ＋ 纯IP)
列表 ＝ “192.168.111.59”
.如果真 (删全部空 (列表) ＝ “” 或 纯IP ＝ “”)
    返回 (假)
.如果真结束
规范化 ＝ “,” ＋ 删全部空 (列表) ＋ “,”
返回 (寻找文本 (规范化, “,” ＋ 纯IP ＋ “,”, , 假) ≠ -1)


.子程序 _按钮_左移_被单击
' 拉伸全屏后不再需要挪图；留空避免画到已释放的屏幕图片句柄
返回 ()


.子程序 _按钮_上移_被单击
返回 ()


.子程序 _按钮_还原_被单击
' 1 若是你原来的还原值可保留；全屏请用拉伸（常见为 2，以属性下拉为准）
画板_显示屏幕信息.底图方式 ＝ 2
.如果真 (取字节集长度 (最近一帧) ＞ 0)
    画板_显示屏幕信息.底图 ＝ 最近一帧
.如果真结束


.子程序 _断开连接_被单击
.局部变量 客户IP, 文本型

是否接收 ＝ 假
客户IP ＝ J_客户IP
输出调试文本 (客户IP)
服务器_局域网监控系统.断开客户 (客户IP)
编辑框_状态.内容 ＝ 编辑框_状态.内容 ＋ #换行符 ＋ “用户【：” ＋ 客户IP ＋ “】已断开！”


.子程序 组件位置大小调整

画板_显示屏幕信息.宽度 ＝ J_屏幕宽度 － 300
画板_显示屏幕信息.高度 ＝ J_屏幕高度 － 100
分组框_系统控制.左边 ＝ 画板_显示屏幕信息.宽度 ＋ 20
分组框_系统管理.左边 ＝ 画板_显示屏幕信息.宽度 ＋ 20
编辑框_状态.左边 ＝ 画板_显示屏幕信息.宽度 ＋ 20


.子程序 __启动窗口_尺寸被改变

组件位置大小调整 ()


.子程序 _接收数据_被单击

是否接收 ＝ 真


.子程序 _画板_显示屏幕信息_绘画
.参数 重画区左边, 整数型
.参数 重画区上边, 整数型
.参数 重画区右边, 整数型
.参数 重画区下边, 整数型
' 不用在这里 StretchBlt；底图方式＝拉伸即可
