.版本 2

' ============================================================
' 修复空白屏：你把 画板.底图＝收图缓冲 注释掉了，所以一定是灰的
' 另：调用压缩保存前必须先拼好 保存路径
' ============================================================

.子程序 _服务器_局域网监控系统_数据到达
.局部变量 数据, 字节集
.局部变量 缓冲长度, 整数型

.如果真 (是否接收 ≠ 真)
    返回 ()
.如果真结束

数据 ＝ 服务器_局域网监控系统.取回数据 ()
.如果真 (取字节集长度 (数据) ＜ 1)
    返回 ()
.如果真结束

收图缓冲 ＝ 收图缓冲 ＋ 数据
缓冲长度 ＝ 取字节集长度 (收图缓冲)

.如果真 (缓冲长度 ＞ 12000000)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束

.如果真 (缓冲长度 ＜ 20000)
    返回 ()
.如果真结束

屏幕图片 ＝ 载入图片 (收图缓冲)
.如果真 (屏幕图片 ＝ 0)
    返回 ()
.如果真结束

.如果真 (取启动时间 () － 上次刷新时间 ＜ 120)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束
上次刷新时间 ＝ 取启动时间 ()

' ★★★ 必须有这一行，否则画面永远空白 ★★★
画板_显示屏幕信息.底图 ＝ 收图缓冲

' ---------- 批量保存（可选）----------
.如果真 (是否保存图片 ＝ 真)
    .如果真 (保存间隔毫秒 ＜ 1)
        保存间隔毫秒 ＝ 1000
    .如果真结束
    .如果真 (取启动时间 () － 上次保存时间 ≥ 保存间隔毫秒)
        .如果真 (保存目录 ＝ “”)
            保存目录 ＝ “E:\旧硬盘资料\原创实例\局域网\SF\”
        .如果真结束
        .如果真 (取文本右边 (保存目录, 1) ≠ “\”)
            保存目录 ＝ 保存目录 ＋ “\”
        .如果真结束
        .如果真 (保存序号 ＜ 1)
            保存序号 ＝ 1
        .如果真结束
        .如果真 (保存JPEG质量 ＜ 1 或 保存JPEG质量 ＞ 100)
            保存JPEG质量 ＝ 60
        .如果真结束

        ' ★ 你原来漏了这行：保存路径一直是空的
        保存路径 ＝ 保存目录 ＋ 到文本 (保存序号) ＋ “.jpg”

        .如果真 (监控收图_压缩保存JPEG (收图缓冲, 保存路径, 保存JPEG质量))
            保存序号 ＝ 保存序号 ＋ 1
            上次保存时间 ＝ 取启动时间 ()
        .如果真结束
    .如果真结束
.如果真结束

' ★ 显示/保存后清空，准备下一帧
收图缓冲 ＝ {  }
