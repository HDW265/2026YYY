.版本 2

' ============================================================
' 收图显示 + 批量保存到目录
'
' 程序集变量请增加：
' .程序集变量 是否保存图片, 逻辑型
' .程序集变量 保存目录, 文本型
' .程序集变量 保存序号, 整数型
' .程序集变量 保存间隔毫秒, 整数型
' .程序集变量 上次保存时间, 整数型
'
' 启动时（创建完毕）示例：
' 是否保存图片 ＝ 真
' 保存目录 ＝ “E:\旧硬盘资料\原创实例\局域网\”
' 保存序号 ＝ 1
' 保存间隔毫秒 ＝ 500   ' 最少隔 500ms 存一张，避免狂写硬盘
' ============================================================

.子程序 _服务器_局域网监控系统_数据到达
.局部变量 数据, 字节集
.局部变量 缓冲长度, 整数型
.局部变量 帧数据, 字节集

.如果真 (是否接收 ≠ 真)
    返回 ()
.如果真结束

数据 ＝ 服务器_局域网监控系统.取回数据 ()
.如果真 (取字节集长度 (数据) ＜ 1)
    返回 ()
.如果真结束

收图缓冲 ＝ 收图缓冲 ＋ 数据
缓冲长度 ＝ 取字节集长度 (收图缓冲)
.如果真 (缓冲长度 ＞ 12000000)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束

帧数据 ＝ 监控收图_取出完整帧 ()
.如果真 (取字节集长度 (帧数据) ＜ 5000)
    返回 ()
.如果真结束

.如果真 (取启动时间 () － 上次刷新时间 ＜ 200)
    返回 ()
.如果真结束
上次刷新时间 ＝ 取启动时间 ()
最近一帧 ＝ 帧数据

画板_显示屏幕信息.底图 ＝ 帧数据

' ★ 批量保存：存完整帧，不要写 收图缓冲（可能半包/含下一帧）
监控收图_批量保存帧 (帧数据)


.子程序 监控收图_批量保存帧
.参数 帧数据, 字节集
.局部变量 文件名, 文本型
.局部变量 完整路径, 文本型

.如果真 (是否保存图片 ≠ 真)
    返回 ()
.如果真结束
.如果真 (取字节集长度 (帧数据) ＜ 1)
    返回 ()
.如果真结束

' 保存降频（可按需要改 保存间隔毫秒）
.如果真 (保存间隔毫秒 ＜ 1)
    保存间隔毫秒 ＝ 500
.如果真结束
.如果真 (取启动时间 () － 上次保存时间 ＜ 保存间隔毫秒)
    返回 ()
.如果真结束

.如果真 (保存目录 ＝ “”)
    保存目录 ＝ “E:\旧硬盘资料\原创实例\局域网\”
.如果真结束
' 目录末尾补 \
.如果真 (取文本右边 (保存目录, 1) ≠ “\”)
    保存目录 ＝ 保存目录 ＋ “\”
.如果真结束

.如果真 (保存序号 ＜ 1)
    保存序号 ＝ 1
.如果真结束

' 1.jpg 2.jpg 3.jpg ... 也可改成时间戳文件名见下方注释
文件名 ＝ 到文本 (保存序号) ＋ “.jpg”
完整路径 ＝ 保存目录 ＋ 文件名

' 目录不存在时写到文件可能失败：请先手动建好目录
写到文件 (完整路径, 帧数据)

保存序号 ＝ 保存序号 ＋ 1
上次保存时间 ＝ 取启动时间 ()

' --- 可选：改用时间戳文件名，避免覆盖、也不用序号 ---
' 完整路径 ＝ 保存目录 ＋ 时间到文本 (取现行时间 (), 2) ＋ “_” ＋ 到文本 (取启动时间 ()) ＋ “.jpg”
' 写到文件 (完整路径, 帧数据)


.子程序 监控收图_取出完整帧, 字节集
.局部变量 缓冲长度, 整数型
.局部变量 起点, 整数型
.局部变量 终点, 整数型
.局部变量 帧数据, 字节集
.局部变量 搜索位置, 整数型
.局部变量 临时, 整数型

帧数据 ＝ {  }
缓冲长度 ＝ 取字节集长度 (收图缓冲)
.如果真 (缓冲长度 ＜ 5000)
    返回 (帧数据)
.如果真结束

起点 ＝ 寻找字节集 (收图缓冲, { 255, 216 }, 1)
.如果真 (起点 ＜ 1)
    .如果真 (缓冲长度 ＞ 65536)
        收图缓冲 ＝ {  }
    .如果真结束
    返回 (帧数据)
.如果真结束

.如果真 (起点 ＞ 1)
    收图缓冲 ＝ 取字节集右边 (收图缓冲, 缓冲长度 － 起点 ＋ 1)
    缓冲长度 ＝ 取字节集长度 (收图缓冲)
.如果真结束

终点 ＝ 0
搜索位置 ＝ 3
.判断循环首 (真)
    临时 ＝ 寻找字节集 (收图缓冲, { 255, 217 }, 搜索位置)
    .如果真 (临时 ＜ 1)
        跳出循环 ()
    .如果真结束
    终点 ＝ 临时
    搜索位置 ＝ 临时 ＋ 2
    .如果真 (搜索位置 ＞ 缓冲长度)
        跳出循环 ()
    .如果真结束
.判断循环尾 ()

.如果真 (终点 ＜ 1)
    返回 (帧数据)
.如果真结束

帧数据 ＝ 取字节集左边 (收图缓冲, 终点 ＋ 1)
.如果真 (终点 ＋ 1 ＜ 缓冲长度)
    收图缓冲 ＝ 取字节集右边 (收图缓冲, 缓冲长度 － 终点 － 1)
.否则
    收图缓冲 ＝ {  }
.如果真结束
返回 (帧数据)
