.版本 2

' ============================================================
' 最小稳定收图（防闪退）
' 1. 只用画板_显示屏幕信息.底图 ＝ 帧数据
' 2. 禁止：载入图片 / DeleteObject / StretchBlt / GetObject / 图片框
' 3. 先能稳定出图；画板不能拉伸，铺满以后再说
' ============================================================

.子程序 _服务器_局域网监控系统_数据到达
.局部变量 数据, 字节集
.局部变量 缓冲长度, 整数型
.局部变量 帧数据, 字节集

.如果真 (是否接收 ≠ 真)
    返回 ()
.如果真结束

数据 ＝ 服务器_局域网监控系统.取回数据 ()
.如果真 (取字节集长度 (数据) ＜ 1)
    返回 ()
.如果真结束

收图缓冲 ＝ 收图缓冲 ＋ 数据
缓冲长度 ＝ 取字节集长度 (收图缓冲)

.如果真 (缓冲长度 ＞ 12000000)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束

帧数据 ＝ 监控收图_取出完整帧 ()
.如果真 (取字节集长度 (帧数据) ＜ 5000)
    返回 ()
.如果真结束

.如果真 (取启动时间 () － 上次刷新时间 ＜ 200)
    返回 ()
.如果真结束
上次刷新时间 ＝ 取启动时间 ()
最近一帧 ＝ 帧数据

' 只保留这一行显示，不要再写载入图片/StretchBlt/DeleteObject/图片框
画板_显示屏幕信息.底图 ＝ 帧数据


.子程序 监控收图_取出完整帧, 字节集
.局部变量 缓冲长度, 整数型
.局部变量 起点, 整数型
.局部变量 终点, 整数型
.局部变量 帧数据, 字节集
.局部变量 搜索位置, 整数型
.局部变量 临时, 整数型

' 只按 JPEG 头尾取帧，不用 RtlMoveMemory（少一条崩溃点）
帧数据 ＝ {  }
缓冲长度 ＝ 取字节集长度 (收图缓冲)
.如果真 (缓冲长度 ＜ 5000)
    返回 (帧数据)
.如果真结束

起点 ＝ 寻找字节集 (收图缓冲, { 255, 216 }, 1)
.如果真 (起点 ＜ 1)
    .如果真 (缓冲长度 ＞ 65536)
        收图缓冲 ＝ {  }
    .如果真结束
    返回 (帧数据)
.如果真结束

.如果真 (起点 ＞ 1)
    收图缓冲 ＝ 取字节集右边 (收图缓冲, 缓冲长度 － 起点 ＋ 1)
    缓冲长度 ＝ 取字节集长度 (收图缓冲)
.如果真结束

' 找最后一个 FFD9，减少半包误判
终点 ＝ 0
搜索位置 ＝ 3
.判断循环首 (真)
    临时 ＝ 寻找字节集 (收图缓冲, { 255, 217 }, 搜索位置)
    .如果真 (临时 ＜ 1)
        跳出循环 ()
    .如果真结束
    终点 ＝ 临时
    搜索位置 ＝ 临时 ＋ 2
    .如果真 (搜索位置 ＞ 缓冲长度)
        跳出循环 ()
    .如果真结束
.判断循环尾 ()

.如果真 (终点 ＜ 1)
    返回 (帧数据)
.如果真结束

帧数据 ＝ 取字节集左边 (收图缓冲, 终点 ＋ 1)
.如果真 (终点 ＋ 1 ＜ 缓冲长度)
    收图缓冲 ＝ 取字节集右边 (收图缓冲, 缓冲长度 － 终点 － 1)
.否则
    收图缓冲 ＝ {  }
.如果真结束
返回 (帧数据)
