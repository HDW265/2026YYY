.版本 2

' ============================================================
' 原版数据到达 + 批量保存 + 保存间隔
'
' 程序集变量：
' .程序集变量 是否接收, 逻辑型
' .程序集变量 屏幕图片, 整数型
' .程序集变量 收图缓冲, 字节集
' .程序集变量 上次刷新时间, 整数型
' .程序集变量 是否保存图片, 逻辑型
' .程序集变量 保存目录, 文本型
' .程序集变量 保存序号, 整数型
' .程序集变量 保存间隔毫秒, 整数型
' .程序集变量 上次保存时间, 整数型
'
' 创建完毕：
' 是否保存图片 ＝ 真
' 保存目录 ＝ “E:\旧硬盘资料\原创实例\局域网\”
' 保存序号 ＝ 1
' 保存间隔毫秒 ＝ 1000   ' 每隔 1000 毫秒(=1秒)存一张；改这里即可
' ============================================================

.子程序 _服务器_局域网监控系统_数据到达
.局部变量 数据, 字节集
.局部变量 缓冲长度, 整数型
.局部变量 保存路径, 文本型

.如果真 (是否接收 ≠ 真)
    返回 ()
.如果真结束

数据 ＝ 服务器_局域网监控系统.取回数据 ()
.如果真 (取字节集长度 (数据) ＜ 1)
    返回 ()
.如果真结束

收图缓冲 ＝ 收图缓冲 ＋ 数据
缓冲长度 ＝ 取字节集长度 (收图缓冲)

.如果真 (缓冲长度 ＞ 12000000)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束

.如果真 (缓冲长度 ＜ 20000)
    返回 ()
.如果真结束

屏幕图片 ＝ 载入图片 (收图缓冲)
.如果真 (屏幕图片 ＝ 0)
    返回 ()
.如果真结束

.如果真 (取启动时间 () － 上次刷新时间 ＜ 120)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束
上次刷新时间 ＝ 取启动时间 ()

画板_显示屏幕信息.底图 ＝ 收图缓冲

' ---------- 批量保存（带间隔）----------
.如果真 (是否保存图片 ＝ 真)
    .如果真 (保存间隔毫秒 ＜ 1)
        保存间隔毫秒 ＝ 1000
    .如果真结束
    ' 未到间隔：只显示，不保存，也不清空缓冲前的图数据（下面仍会清空）
    .如果真 (取启动时间 () － 上次保存时间 ≥ 保存间隔毫秒)
        .如果真 (保存目录 ＝ “”)
            保存目录 ＝ “E:\旧硬盘资料\原创实例\局域网\”
        .如果真结束
        .如果真 (取文本右边 (保存目录, 1) ≠ “\”)
            保存目录 ＝ 保存目录 ＋ “\”
        .如果真结束
        .如果真 (保存序号 ＜ 1)
            保存序号 ＝ 1
        .如果真结束

        保存路径 ＝ 保存目录 ＋ 到文本 (保存序号) ＋ “.jpg”
        写到文件 (保存路径, 收图缓冲)
        保存序号 ＝ 保存序号 ＋ 1
        上次保存时间 ＝ 取启动时间 ()
    .如果真结束
.如果真结束

收图缓冲 ＝ {  }
