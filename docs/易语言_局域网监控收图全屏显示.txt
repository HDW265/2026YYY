.版本 2

' ============================================================
' 你当前的错误写法（没有 .否则）：
' .如果真 (监控收图_压缩保存JPEG (...))
'     成功输出
'     失败保存      ← 这些都在“成功”分支里
' .如果真结束
' 压缩一返回假，就什么都不存、也没有调试输出。
' ============================================================

.子程序 __启动窗口_创建完毕

服务器_局域网监控系统.端口 ＝ 到整数 (_启动窗口.端口_编辑框)
是否接收 ＝ 真
J_屏幕宽度 ＝ 取屏幕宽度 ()
J_屏幕高度 ＝ 取屏幕高度 ()
画板_显示屏幕信息.顶边 ＝ 10
画板_显示屏幕信息.左边 ＝ 10

是否保存图片 ＝ 真
保存目录 ＝ “E:\旧硬盘资料\原创实例\局域网\SF\”
保存序号 ＝ 1
保存间隔毫秒 ＝ 1000
保存JPEG质量 ＝ 60
上次保存时间 ＝ 0


.子程序 _服务器_局域网监控系统_数据到达
.局部变量 数据, 字节集
.局部变量 缓冲长度, 整数型

.如果真 (是否接收 ≠ 真)
    返回 ()
.如果真结束

数据 ＝ 服务器_局域网监控系统.取回数据 ()
.如果真 (取字节集长度 (数据) ＜ 1)
    返回 ()
.如果真结束

收图缓冲 ＝ 收图缓冲 ＋ 数据
缓冲长度 ＝ 取字节集长度 (收图缓冲)

.如果真 (缓冲长度 ＞ 12000000)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束
.如果真 (缓冲长度 ＜ 20000)
    返回 ()
.如果真结束

屏幕图片 ＝ 载入图片 (收图缓冲)
.如果真 (屏幕图片 ＝ 0)
    返回 ()
.如果真结束

.如果真 (取启动时间 () － 上次刷新时间 ＜ 120)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束
上次刷新时间 ＝ 取启动时间 ()

画板_显示屏幕信息.底图 ＝ 收图缓冲
输出调试文本 (“已显示 大小=” ＋ 到文本 (缓冲长度))

.如果真 (是否保存图片 ≠ 真)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束

.如果真 (保存间隔毫秒 ＜ 1)
    保存间隔毫秒 ＝ 1000
.如果真结束
.如果真 (取启动时间 () － 上次保存时间 ＜ 保存间隔毫秒)
    收图缓冲 ＝ {  }
    返回 ()
.如果真结束

.如果真 (保存目录 ＝ “”)
    保存目录 ＝ “E:\旧硬盘资料\原创实例\局域网\SF\”
.如果真结束
.如果真 (取文本右边 (保存目录, 1) ≠ “\”)
    保存目录 ＝ 保存目录 ＋ “\”
.如果真结束
.如果真 (保存序号 ＜ 1)
    保存序号 ＝ 1
.如果真结束
.如果真 (保存JPEG质量 ＜ 1 或 保存JPEG质量 ＞ 100)
    保存JPEG质量 ＝ 60
.如果真结束

' ★★★ 正确写法：成功 / 否则失败，必须分开 ★★★
保存路径 ＝ 保存目录 ＋ 到文本 (保存序号) ＋ “.jpg”
.如果真 (监控收图_压缩保存JPEG (收图缓冲, 保存路径, 保存JPEG质量))
    输出调试文本 (“JPEG保存成功：” ＋ 保存路径)
.否则
    保存路径 ＝ 保存目录 ＋ 到文本 (保存序号) ＋ “.bmp”
    写到文件 (保存路径, 收图缓冲)
    输出调试文本 (“JPEG失败，已原样保存：” ＋ 保存路径)
.如果真结束

保存序号 ＝ 保存序号 ＋ 1
上次保存时间 ＝ 取启动时间 ()
收图缓冲 ＝ {  }
