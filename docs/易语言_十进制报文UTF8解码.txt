.版本 2

' ============================================================
' 十进制报文 → 字节集 → UTF8扫描解码（对齐 Python decode + 去控制符）
' 用法：
'   1. 新建易语言窗口程序，把下面 DLL命令 + 子程序全部粘贴进程序
'   2. 在按钮或启动事件里调用：测试_十进制报文解码 ()
' 说明：
'   - 全程用“字节集”，不要对含 0 的数据用 到文本()
'   - 包内中文是 UTF-8，不是易语言默认的 GBK/ANSI
'   - 非法 UTF-8 字节直接跳过，等同 Python: errors="ignore"
' ============================================================

.DLL命令 MultiByteToWideChar, 整数型, "kernel32.dll", "MultiByteToWideChar"
    .参数 CodePage, 整数型
    .参数 dwFlags, 整数型
    .参数 lpMultiByteStr, 字节集
    .参数 cbMultiByte, 整数型
    .参数 lpWideCharStr, 字节集
    .参数 cchWideChar, 整数型

.DLL命令 WideCharToMultiByte, 整数型, "kernel32.dll", "WideCharToMultiByte"
    .参数 CodePage, 整数型
    .参数 dwFlags, 整数型
    .参数 lpWideCharStr, 字节集
    .参数 cchWideChar, 整数型
    .参数 lpMultiByteStr, 字节集
    .参数 cchMultiByte, 整数型
    .参数 lpDefaultChar, 整数型
    .参数 lpUsedDefaultChar, 整数型

' 备注：下面转换子程序一次给足缓冲区，避免易语言里给 DLL 传空指针不稳定

.子程序 测试_十进制报文解码
.局部变量 十进制文本, 文本型
.局部变量 明文, 文本型

十进制文本 ＝ “243,4,0,0,15,3,115,0,27,229,164,167,229,132,146,50,53,57,51,55,48,57,40,231,130,185,229,135,187,230,148,185,229,144,141,41,5,105,0,0,0,16,6,108,0,0,0,0,0,0,0,0,7,108,0,0,0,0,0,0,58,86,2,105,0,45,159,32,37,115,0,8,87,90,52,52,54,229,140,186,40,115,0,0,41,98,1,43,115,0,6,233,187,152,232,174,164,45,115,0,0,46,98,1,44,98,1,20,108,0,0,0,0,0,0,48,248,38,115,0,13,228,184,190,228,186,186,229,162,131,54,233,152,182,255,107,35,40”
明文 ＝ 十进制报文到明文 (十进制文本)
信息框 (“===== UTF8明文解析结果 =====” ＋ #换行符 ＋ 明文, 0, “解码结果”)
' 预期含有：大儒2593709(点击改名)、WZ446区、默认、举人境6阶

.子程序 十进制报文到明文, 文本型, 公开, , 逗号分隔十进制 → 过滤控制符后的可见明文
.参数 十进制文本, 文本型
.局部变量 数据, 字节集

数据 ＝ 十进制文本到字节集 (十进制文本)
返回 (字节集UTF8扫描到文本 (数据))

.子程序 十进制文本到字节集, 字节集, 公开, , 把 "243,4,0,0,15" 转成原始字节集
.参数 十进制文本, 文本型
.局部变量 成员, 文本型, , "0"
.局部变量 数据, 字节集
.局部变量 i, 整数型
.局部变量 段, 文本型
.局部变量 值, 整数型

数据 ＝ {}
.如果真 (删首尾空 (十进制文本) ＝ “”)
    返回 (数据)
.如果真结束

成员 ＝ 分割文本 (十进制文本, “,”)
.计次循环首 (取数组成员数 (成员), i)
    段 ＝ 删首尾空 (成员 [i])
    .如果真 (段 ≠ “”)
        值 ＝ 到整数 (段)
        .如果真 (值 ＜ 0 或 值 ＞ 255)
            ' 非法十进制，直接跳过；也可改成返回空字节集报错
        .否则
            数据 ＝ 数据 ＋ { 值 }
        .如果真结束
    .如果真结束
.计次循环尾 ()
返回 (数据)

.子程序 字节集UTF8扫描到文本, 文本型, 公开, , 按UTF-8扫描，非法字节忽略，并去掉控制字符
.参数 数据, 字节集
.局部变量 结果, 文本型
.局部变量 i, 整数型
.局部变量 总长, 整数型
.局部变量 b1, 整数型
.局部变量 b2, 整数型
.局部变量 b3, 整数型
.局部变量 b4, 整数型
.局部变量 一段, 字节集
.局部变量 一段文本, 文本型
.局部变量 码, 整数型

结果 ＝ “”
总长 ＝ 取字节集长度 (数据)
i ＝ 1
.判断循环首 (i ≤ 总长)
    b1 ＝ 取字节集数据 (数据, i)

    ' ---------- 1 字节 ASCII ----------
    .如果真 (b1 ≤ 127)
        ' 对齐 Python clean_visible：去掉 0-31 和 127
        .如果真 (b1 ≥ 32 且 b1 ≠ 127)
            结果 ＝ 结果 ＋ 字符 (b1)
        .如果真结束
        i ＝ i ＋ 1
        到循环尾 ()
    .如果真结束

    ' ---------- 续字节单独出现：跳过 ----------
    .如果真 (是否UTF8续字节 (b1))
        i ＝ i ＋ 1
        到循环尾 ()
    .如果真结束

    ' ---------- 2 字节 UTF-8 ----------
    .如果真 (b1 ≥ 192 且 b1 ≤ 223)
        .如果真 (b1 ≥ 194 且 i ＋ 1 ≤ 总长)
            b2 ＝ 取字节集数据 (数据, i ＋ 1)
            .如果真 (是否UTF8续字节 (b2))
                一段 ＝ 取字节集中间 (数据, i, 2)
                一段文本 ＝ UTF8字节集到本地文本 (一段)
                .如果真 (一段文本 ≠ “”)
                    码 ＝ 取代码 (取文本左边 (一段文本, 1))
                    .如果真 (码 ＞ 31 且 码 ≠ 127)
                        结果 ＝ 结果 ＋ 一段文本
                    .如果真结束
                    i ＝ i ＋ 2
                    到循环尾 ()
                .如果真结束
            .如果真结束
        .如果真结束
        i ＝ i ＋ 1
        到循环尾 ()
    .如果真结束

    ' ---------- 3 字节 UTF-8（中文常见）----------
    .如果真 (b1 ≥ 224 且 b1 ≤ 239)
        .如果真 (i ＋ 2 ≤ 总长)
            b2 ＝ 取字节集数据 (数据, i ＋ 1)
            b3 ＝ 取字节集数据 (数据, i ＋ 2)
            .如果真 (是否UTF8续字节 (b2) 且 是否UTF8续字节 (b3))
                一段 ＝ 取字节集中间 (数据, i, 3)
                一段文本 ＝ UTF8字节集到本地文本 (一段)
                .如果真 (一段文本 ≠ “”)
                    码 ＝ 取代码 (取文本左边 (一段文本, 1))
                    .如果真 (码 ＞ 31 且 码 ≠ 127)
                        结果 ＝ 结果 ＋ 一段文本
                    .如果真结束
                    i ＝ i ＋ 3
                    到循环尾 ()
                .如果真结束
            .如果真结束
        .如果真结束
        i ＝ i ＋ 1
        到循环尾 ()
    .如果真结束

    ' ---------- 4 字节 UTF-8 ----------
    .如果真 (b1 ≥ 240 且 b1 ≤ 244)
        .如果真 (i ＋ 3 ≤ 总长)
            b2 ＝ 取字节集数据 (数据, i ＋ 1)
            b3 ＝ 取字节集数据 (数据, i ＋ 2)
            b4 ＝ 取字节集数据 (数据, i ＋ 3)
            .如果真 (是否UTF8续字节 (b2) 且 是否UTF8续字节 (b3) 且 是否UTF8续字节 (b4))
                一段 ＝ 取字节集中间 (数据, i, 4)
                一段文本 ＝ UTF8字节集到本地文本 (一段)
                .如果真 (一段文本 ≠ “”)
                    码 ＝ 取代码 (取文本左边 (一段文本, 1))
                    .如果真 (码 ＞ 31 且 码 ≠ 127)
                        结果 ＝ 结果 ＋ 一段文本
                    .如果真结束
                    i ＝ i ＋ 4
                    到循环尾 ()
                .如果真结束
            .如果真结束
        .如果真结束
        i ＝ i ＋ 1
        到循环尾 ()
    .如果真结束

    ' 其它非法起始字节
    i ＝ i ＋ 1
.判断循环尾 ()
返回 (结果)

.子程序 是否UTF8续字节, 逻辑型
.参数 值, 整数型
返回 (值 ≥ 128 且 值 ≤ 191)

.子程序 UTF8字节集到本地文本, 文本型, , , UTF-8字节集 → 易语言本地文本(ANSI/GBK)
.参数 utf8数据, 字节集
.局部变量 utf8长, 整数型
.局部变量 宽长度, 整数型
.局部变量 宽字节集, 字节集
.局部变量 ansi长度, 整数型
.局部变量 ansi字节集, 字节集

utf8长 ＝ 取字节集长度 (utf8数据)
.如果真 (utf8长 ≤ 0)
    返回 (“”)
.如果真结束

' CP_UTF8 = 65001；宽字符个数不会超过 utf8 字节数
宽字节集 ＝ 取空白字节集 (utf8长 × 2)
宽长度 ＝ MultiByteToWideChar (65001, 0, utf8数据, utf8长, 宽字节集, utf8长)
.如果真 (宽长度 ≤ 0)
    返回 (“”)
.如果真结束

' CP_ACP = 0；GBK 下每字符最多约 2 字节，这里按 3 倍留余量
ansi字节集 ＝ 取空白字节集 (宽长度 × 3)
ansi长度 ＝ WideCharToMultiByte (0, 0, 宽字节集, 宽长度, ansi字节集, 取字节集长度 (ansi字节集), 0, 0)
.如果真 (ansi长度 ≤ 0)
    返回 (“”)
.如果真结束

' 注意：这里是“一小段合法UTF8”转出来的ANSI，一般不含 0，可以到文本
返回 (到文本 (取字节集左边 (ansi字节集, ansi长度)))

' ============================================================
' 若已装精易模块，也可把 UTF8字节集到本地文本 整段换成：
'   返回 (编码_Utf8到Ansi (utf8数据))
' 切记：仍然必须先“字节集扫描”，不要对整包含0数据直接当文本处理。
' ============================================================
